<!DOCTYPE html>
<html>
  <head>
    <title>VariablesApp</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div>
      <div>
        <div>
          <div class="nav-collapse">
            <% if @current_user %>
              <div>
                <div><b>Cadastro</b></div>
                <% if @current_user.profile == User::ROOM1 %>
                  <div><%= link_to 'Origem', origins_path %></div>
                  <div><%= link_to 'Campanha', campaigns_path %></div>
                  <div><%= link_to 'Variável', variables_path %></div>
                <% end %>

                <% if @current_user.profile == User::ROOM2 %>
                  <div><%= link_to 'Origem', origins_path %></div>
                  <div><%= link_to 'Variável', variables_path %></div>
                  <div><%= link_to 'Tabela', tables_path %></div>
                  <div><%= link_to 'Processo', processids_path %></div>
                <% end %>

                <% if @current_user.admin? %>
                  <div><%= link_to ">> Usuários", users_path %></div>
                <% end %>
              </div>

              <div align="right">
                <div>
                  <b>Portal de Variáveis</b>
                  <b><%= @current_user.profile %></b>
                  <%= link_to @current_user.name, edit_user_path(@current_user) %>
                  <%= link_to "Alterar senha", password_path %> |
                  <%= link_to "Sair", logout_path %>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
    <div>
      <% if @current_user %>
        <div>
          <div><%= link_to @current_user.name, users_path %> <span class="divider">/</span>
            <b><%= t("activerecord.models.#{controller_name.singularize.downcase}.other") %></b></div>
        </div>
      <% end %>
      <%= yield %>
    </div>
    <footer>
      <div>
        <br/>
        <p> </p>
      </div>
    </footer>
  </body>
</html>
